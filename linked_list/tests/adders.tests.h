/* Tests to check if a element is added to the beggining
 * of a list. */
void test_Ll_addFirst();

/* Tests to check if a element is added at the last position
 * of a list. */
void test_Ll_addLast();

/* Tests to check if a element is added to a certain index 
 * of a list. */
void test_Ll_addAt();
