#pragma once
#include "../queue.h"
#include "../../LinkedList/linkedList.h"

/* @brief Tests if the functions for enqueue and dequeue works
 * when adding and removing elements from a queue. */
void test_Q_enqueue_and_Q_dequeue();

/** @brief Tests if the function detects if the queue is empty 
 * or not. */
void test_Q_isEmpty();

/** @brief Tests if the function clears a Queue 
 * correctly */
void test_Q_clear();
